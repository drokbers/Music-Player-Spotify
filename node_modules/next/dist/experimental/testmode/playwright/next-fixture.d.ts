import type { Page, TestInfo } from '@playwright/test';
import type { NextWorkerFixture, FetchHandler } from './next-worker-fixture';
export interface NextFixture {
    onFetch: (handler: FetchHandler) => void;
}
export declare function applyNextFixture(use: (fixture: NextFixture) => Promise<void>, { testInfo, nextWorker, page, extraHTTPHeaders, }: {
    testInfo: TestInfo;
    nextWorker: NextWorkerFixture;
    page: Page;
    extraHTTPHeaders: Record<string, string> | undefined;
}): Promise<void>;
